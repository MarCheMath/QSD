#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='Queer Speeddating Solver',
      version='1.0',
      description='Two solvers for the scheduling problem of queer speeddating',
      author='Mark C',
      packages=find_packages(),
     )